'use strict';

/**
 * page-company-value router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::page-company-value.page-company-value');
